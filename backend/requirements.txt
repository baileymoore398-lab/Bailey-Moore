# --- Web framework ---
# fastapi 0.115 pulls starlette >=0.40 (fixes the 0.37.2 CVEs).
fastapi==0.115.6
uvicorn[standard]==0.30.1
python-multipart==0.0.9
pydantic==2.7.4
pydantic-settings==2.3.4
# Required by Pydantic's EmailStr (used in schemas.py / contact.py). Pin it
# explicitly so a clean prod build never crashes on import for lack of it.
email-validator==2.3.0

# --- Database ---
sqlalchemy==2.0.31
alembic==1.13.2
psycopg2-binary==2.9.9

# --- Auth / security ---
bcrypt==4.1.3
python-jose[cryptography]==3.4.0  # fixes PYSEC-2024-233 / 2025-185
slowapi==0.1.9
defusedxml==0.7.1  # hardened XML parsing (entity-expansion DoS)

# --- Async tasks ---
celery==5.4.0
redis==5.0.7

# --- Storage ---
boto3==1.34.140

# --- Geo / numeric ---
numpy==1.26.4
scipy==1.13.1
gpxpy==1.6.2

# --- Computer vision (map photo AI) ---
opencv-python-headless==4.10.0.84
pillow==11.3.0  # fixes the 10.4.0 CVEs

# --- AI ---
openai==1.35.10

# --- Billing ---
stripe==10.1.0

# --- Observability ---
sentry-sdk[fastapi]==2.20.0

# --- Misc ---
python-dateutil==2.9.0.post0
httpx==0.27.0

# --- Dev / test ---
pytest==8.2.2
ruff==0.5.1
